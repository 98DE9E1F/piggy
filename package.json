{
  "name": "piggy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "lerna run start --scope app --stream",
    "test": "lerna run test --scope app --stream",
    "build": "lerna run build --scope app --stream",
    "eject": "lerna run eject --scope app --stream"
  },
  "workspaces": [
    "packages/app"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nefk/piggy.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/nefk/piggy/issues"
  },
  "homepage": "https://github.com/nefk/piggy#readme",
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
